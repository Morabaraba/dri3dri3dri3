/* global $ _ */
(function() {
	var app = window.app = window.app || {};
	
	app.class = app.class || {};
	app.utils = app.utils || {};
	app.routes = app.routes || {};
	app.mounts = app.mounts || {};
	app.wamp = app.wamp || {};
	app.state = app.state || {};

})();
